/**
 * Chat metadata definition
 */
export default interface MetaDefinition {

    /** Content of context hint */
    context: string;

    /** Should context to be used in request */
    enabled: boolean;
}
